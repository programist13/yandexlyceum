arr = [1, 0, 2, 9, 3, 8, 4, 7, 5, 6]
arr.sort()  # Метод sort() — принимает значения, меняет порядок не сохраняя исходный список.
print(arr)
print(sorted(arr))    # выполняет сортировку элементов списка и сохраняет новые значения в качестве копии.
# Исходный список остается в том же виде.
